# Obsidian Hub 实施清单

> 当前阶段：启动器核心版实施中（合并 M1 与第一批 M2）
> 本清单只覆盖已确认的 Windows 11 MVP。

## 使用规则

- 状态使用：`[ ]` 未开始、`[-]` 进行中、`[x]` 完成、`[!]` 阻塞。
- 每项完成必须同时满足代码、测试和验收条件。
- 任何新增范围先更新 `docs/plan/plan.md`，再进入本清单。
- 不为了“以后可能需要”提前实现 M4 之后的能力。

## M0：技术探针

目标：在正式搭建产品架构前，消除 Windows 与 Obsidian 集成风险。

- [x] 检查本机 Node.js、pnpm、Rust、Cargo、MSVC Build Tools 和 WebView2 环境。
- [x] 确认脚手架的稳定版本与锁定策略。
- [x] 创建最小 Tauri 2 + React + TypeScript 探针。
- [ ] 验证 Windows 原生目录选择器。
- [x] 验证 Rust 端目录存在性及 `.obsidian` 标记目录检查。
- [x] 验证 `obsidian://open?vault=<name>` 的正确 URI 编码。
- [x] 验证使用 Vault ID 打开仓库。
- [ ] 验证 Obsidian 未运行时的启动行为。
- [ ] 验证 Obsidian 已运行且当前为其他仓库时的行为。
- [ ] 验证目标仓库未注册或 URI handler 不存在时的行为。
- [x] 确认 Tauri capability 可以只向前端开放必要能力，URI 启动封装在 Rust 内。
- [x] 记录探针结果并更新规划中的开放问题。

验收：关键系统行为有实机结论，技术方案不存在未说明的高风险假设。

## M1：端到端可用纵切

目标：完成“添加 -> 保存 -> 展示 -> 打开”的最小闭环。

### 项目基础

- [x] 建立 pnpm、Vite、React、TypeScript 和 Tauri 2 工程。
- [x] 配置格式化、Lint、类型检查、前端测试和 Rust 测试命令。
- [x] 建立 `src/` 与 `src-tauri/src/` 的职责边界。
- [x] 配置最小 Tauri capabilities。
- [x] 增加统一错误 DTO 与前端错误映射。

### 数据层

- [x] 定义 `VaultEntry`、`Preferences` 和 `AppConfigV1` 类型。
- [ ] 定义 `VaultRepository` 接口。
- [x] 实现 AppData 配置文件读取。
- [x] 实现配置默认值与 schema 校验。
- [x] 实现临时文件加替换的原子写入。
- [x] 增加损坏配置和不支持版本的错误处理。

### 仓库添加

- [x] 实现系统目录选择。
- [x] 实现 Windows 路径规范化。
- [x] 校验目录存在且包含 `.obsidian`。
- [x] 实现不区分大小写的重复路径检查。
- [x] 用目录名预填展示名称。
- [x] 完成最小添加仓库对话框。

### 仓库启动

- [ ] 定义 `VaultLauncher` 接口。
- [x] 实现 Vault ID 优先、名称降级的 URI 构造。
- [x] 对所有 URI 参数执行标准编码。
- [x] 实现受限的系统 URI 调用。
- [x] 仅在系统成功接收 URI 后更新 `lastOpenedAt`。
- [x] 成功后按偏好关闭 Hub，失败时保留错误状态。

### 纵切验收

- [ ] 首次启动可添加一个真实仓库。
- [ ] 重启 Hub 后仓库仍存在。
- [ ] 点击仓库可启动正确的 Obsidian 仓库。
- [x] 无效路径不会调用 URI。
- [x] 单元测试、类型检查、Lint 和 Debug 构建通过。

## M2：完整 MVP

目标：补齐高频使用体验和完整异常状态。

### 仓库首页

- [x] 将启动页调整为单列居中的空间选择大厅。
- [x] 放大每日开场文案与本地时间，并增加底部每日文案。
- [x] 移除右侧仓库宇宙图，保持仓库列表为页面主体。
- [x] 实现顶部栏、搜索框、仓库列表和添加入口。
- [x] 实现首次启动空状态。
- [x] 实现无搜索结果状态。
- [x] 实现收藏与取消收藏。
- [x] 实现“收藏优先、最近打开其次”的稳定排序。
- [x] 路径失效时在卡片中展示状态与修复入口。
- [x] 同名仓库始终显示路径以消除歧义。

### 管理能力

- [ ] 编辑名称、路径和描述。
- [ ] 编辑路径时重新执行完整校验。
- [x] 实现“从 Hub 移除”确认对话框。
- [x] 明确说明移除不会删除仓库文件。
- [ ] 处理编辑后与已有路径重复的情况。

### 键盘与无障碍

- [x] 启动后自动聚焦搜索框。
- [x] 实现上下方向键移动选择。
- [x] 实现回车打开当前选择。
- [ ] 实现 `/`、`Ctrl+K`、`Ctrl+N`、`Ctrl+,` 和 `Escape`。
- [x] 为按钮、输入框、列表和对话框提供可访问名称与语义。
- [x] 确保焦点态在深浅主题下清晰可见。
- [ ] 检查键盘焦点不会陷入或丢失。

### 视觉系统

- [x] 建立独立设计令牌和深浅主题。
- [x] 支持跟随系统主题。
- [x] 建立紧凑仓库卡片、按钮、输入框、菜单和对话框样式。
- [x] 不依赖 Obsidian 安装文件、内部 CSS 或商标素材。
- [ ] 检查 100%、125%、150% Windows 缩放。
- [ ] 若已引入 StyleSeed，执行 `/ss-review` 并处理问题。
- [x] 统一仓库列表，取消最近仓库与收藏仓库的独立大卡片。
- [x] 增加每日入口语、本地时段图标和选中仓库联动星图。
- [x] 增加稳定的仓库颜色标记，并保持强调色只服务于交互状态。
- [x] 隐藏仓库列表中的本地路径，同时保留路径搜索和管理能力。
- [x] 增加缓存优先、后台刷新的库概览统计组件。
- [x] 调整概览指标顺序，强化日期时间，并以自有弹出菜单替换原生排序下拉框。
- [x] 优化全屏空间利用，并将仓库星图升级为大型中心晶体视觉舞台。
- [x] 增加使用真实日期与概览数据的每日空间海报。
- [x] 在时间区域增加仅标记当天的极简周历。
- [x] 增加添加、刷新、随机选择和打开所选的快捷入口。
- [x] 调整仓库列表间距、操作显隐与选中状态，保持列表同权重。

### 设置与反馈

- [ ] 支持主题模式设置。
- [ ] 支持启动成功后关闭窗口设置。
- [ ] 展示 Obsidian URI 不可用的引导。
- [x] 为稳定错误码提供用户可操作提示。
- [ ] 日志过滤笔记内容、密钥与无必要完整路径。

## M3：质量与本地交付验证

目标：达到可长期自用的稳定程度，不在此阶段公开发布。

### 自动化验证

- [ ] 覆盖路径规范化、判重和 Unicode 边界。
- [ ] 覆盖搜索、收藏和最近使用排序。
- [ ] 覆盖 URI 编码、Vault ID 优先级与 scheme 限制。
- [ ] 覆盖配置解析、原子写入和失败保护。
- [ ] 覆盖核心组件与键盘流程。
- [ ] 确保测试不依赖真实用户仓库。

### Windows 11 实机验收

- [ ] 未安装 Obsidian 时提示正确。
- [ ] Obsidian 未运行时打开正确仓库。
- [ ] Obsidian 已运行时切换到正确仓库。
- [ ] 中文、空格、`#`、`&` 等特殊字符名称工作正确。
- [ ] 同名仓库不会静默打开错误目标。
- [ ] 移动、重命名、离线磁盘等路径失效状态正确。
- [ ] 100 个模拟仓库下搜索和导航流畅。
- [ ] 验证本地 Debug 安装包或可执行文件。

### 完成清理

- [ ] 运行格式检查、Lint、类型检查、测试与 Debug 构建。
- [ ] 清理 TODO、调试输出、死代码、无效 import 和旧实现。
- [ ] 检查 Tauri capabilities 无超范围权限。
- [ ] 更新 README 使用说明和已知限制。
- [ ] 更新 `docs/plan/plan.md` 的实际技术决策。
- [ ] 确认未混入 npm/yarn 锁文件或构建产物。

## M4：一周真实使用复盘

目标：用真实行为决定后续方向。

- [ ] 固定 Hub 到 Windows 任务栏并替代原 Obsidian 入口。
- [ ] 连续使用至少 7 天。
- [ ] 记录启动失败、误选、路径失效和体验摩擦。
- [ ] 记录是否仍频繁直接点击 Obsidian 图标及原因。
- [ ] 评估是否需要托盘、全局快捷键或自动扫描。
- [ ] 根据证据确定下一阶段，只选择一个主方向。

## 暂不排期

- [ ] 指定根目录扫描。
- [ ] 导入 Obsidian 已注册仓库。
- [ ] 系统托盘与全局快捷键。
- [ ] 快速创建、搜索和每日笔记动作。
- [ ] 跨仓库全文搜索。
- [ ] Git、同步、插件与容量状态。
- [ ] 自动更新、代码签名与公开发布。
- [ ] macOS 与 Linux 支持。

## M5：跨仓库链接 Bridge（已确认，实施中）

目标：实现 `@仓库[[笔记]]` 从输入、搜索、插入到跨仓库打开的最小闭环，并由 Hub 管理 Bridge 的本地部署。

### 技术验证

- [ ] 建立独立 Bridge 插件包并验证 Obsidian 可加载最小插件。
- [ ] 验证 `@仓库[[笔记]]` 在 Live Preview、Source mode 与 Reading mode 中的实际解析结果。
- [ ] 验证插件 API 下的补全、编辑器装饰和阅读模式渲染边界。
- [ ] 验证 `obsidian://open` 对仓库名、文件路径及特殊字符的行为。

### 语法与解析

- [x] 定义 `@仓库[[相对路径|别名]]` 的解析器与序列化器。
- [x] 保证普通 `[[...]]` 与跨仓库链接不混淆，并覆盖不完整输入与多链接解析。
- [ ] 为缺失仓库、缺失笔记、歧义仓库和非法语法提供稳定错误状态。
- [ ] 覆盖中文、空格、嵌套目录、`#`、`&` 与转义边界测试。

### 跨仓库搜索与编辑体验

- [x] Hub 只读建立已管理仓库的 Markdown 标题与相对路径索引。
- [x] Bridge 提供“先选仓库、再搜笔记”的两阶段候选列表，明确展示仓库、标题和路径。
- [x] 普通双括号完全恢复 Obsidian 原生补全；跨仓库改用 `@Vault[[Note]]` 局部上下文。
- [x] 选择候选后插入规范化三括号语法。
- [x] 编辑模式显示跨仓库链接装饰，阅读模式渲染为可访问链接。
- [x] 点击链接打开目标仓库和笔记，失败时通过 Obsidian URI 降级并提示。

### Bridge 部署

- [x] 在 Hub 构建中携带 Bridge 的 `manifest.json`、`main.js` 和 `styles.css`。
- [-] Rust 实现 Bridge 安装；状态展示、版本比较与原子替换留待下一轮。
- [ ] 增加“自动为新仓库安装 Bridge”偏好及单仓库安装状态。
- [x] 添加仓库后逐次询问授权安装；失败不回滚仓库添加。
- [x] 不自动关闭受限模式，不修改 Obsidian 社区插件启用列表。
- [x] 提供安装后手动启用 Bridge 的清晰引导。
- [x] 在现有仓库条目展示 Bridge 状态，并提供安装、更新和重试入口。
- [x] 使用插件 manifest 与最近心跳区分未安装、等待启用、已连接和可更新。

### 验收与暂缓项

- [ ] 在两个真实测试仓库间完成输入、插入、点击跳转闭环。
- [ ] 运行插件与 Hub 的格式、Lint、类型、测试和 Debug 构建。
- [ ] 检查仓库写入范围只限 Bridge 插件目录。
- [ ] 暂缓跨仓库反链、嵌入、标题/块引用、稳定笔记 ID、自动修复和全局图谱。

### 跨仓库只读预览

- [x] 增加受认证的单笔记正文读取接口，并限制 Markdown 类型、路径和文件大小。
- [x] Bridge 增加单实例只读预览浮窗，关闭时释放正文与渲染资源。
- [x] 默认点击打开预览，保留“在目标仓库打开”的显式操作。
- [x] 彻底移除跨仓库链接箭头、背景图和主题伪元素。
- [x] 覆盖正文读取边界、预览失败状态与构建验证。

### 跨仓库分层浏览

- [x] 增加当前目录浏览协议与 Hub API，从索引推导直接子文件夹和文件。
- [x] 当前目录输入关键词时递归过滤，并返回总数与分页状态。
- [x] Bridge 候选状态机支持进入文件夹、返回上级和加载更多。
- [x] 文件夹优先排序，保持普通 `[[...]]` 与最终链接语法不变。
- [x] 覆盖目录边界、递归搜索、分页和输入上下文测试并重建应用。
